import { LazyLoadedWidgets } from './app-routing-lazy.ts';

describe('AppRoutingLazy.Ts', () => {
  it('should create an instance', () => {
    // expect(new AppRoutingLazy.Ts()).toBeTruthy();
  });
});
