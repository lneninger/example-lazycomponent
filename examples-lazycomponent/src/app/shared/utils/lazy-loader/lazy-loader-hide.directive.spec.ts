import { LazyLoaderHideDirective } from './lazy-loader-hide.directive';

describe('LazyLoaderHideDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyLoaderHideDirective();
    expect(directive).toBeTruthy();
  });
});
