import { LazyLoaderInjector } from './lazy-loader-injector';

describe('LazyLoaderInjector', () => {
  it('should create an instance', () => {
    expect(new LazyLoaderInjector()).toBeTruthy();
  });
});
